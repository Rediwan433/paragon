# Ghostty
Paragon theme for Ghostty terminal.

1. Download the file `paragon.conf` to the `themes/` subdirectory of your Ghostty configuration (i.e. `~/.config/ghostty/themes/`).
2. Set `theme = paragon.conf` in your Ghostty configuration file.
3. Reload or restart Ghostty.
