# OpenCode
Paragon theme for OpenCode AI coding assistant.

1. Create the themes directory for opencode at `./config/opencode/themes`
2. Paste the `paragon.json` file into the themes directory.
3. Launch opencode and select paragon as your theme.
