# Kitty
Paragon theme for Kitty terminal.

1. Create folder to store the themes for kitty:
    ```bash
    mkdir -p ~/.config/kitty/themes/
    ```

2. Copy `paragon.conf` to ~/.config/kitty/themes/

3. Set theme into `kitty.conf`:
    ```
    include paragon.conf
    ```
