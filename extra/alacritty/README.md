# Alacritty
Paragon theme for Alacritty terminal emulator.

1. Copy `paragon.toml` to your Alacritty configuration directory:
2. Add to your `alacritty.toml`:
    ```toml
    general.import = [
    "~/.config/alacritty/paragon.toml"
    ]
    ```
